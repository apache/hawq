<ivysettings>

  <!-- ====================================================================== -->
       
  <settings defaultResolver="shared" />

  <!-- ====================================================================== -->

  <caches defaultCacheDir="/opt/releng/tools"
          lockStrategy="artifact-lock"
          ivyPattern="[organisation]/[module]/ivy-[revision].xml"
          artifactPattern="[organisation]/[module]/[type]s/[artifact]-[revision].[ext]"
          datafilePattern="[organisation]/[module]/ivydata-[revision].properties"
          />

  <!-- ====================================================================== -->

  <resolvers>
    <filesystem name="local">
      <ivy      pattern="/opt/ivy/modules/[organisation]/[module]/ivy-[revision].xml" />
      <artifact pattern="/opt/ivy/modules/[organisation]/[module]/[type]s/[artifact]-[revision].[ext]" />
    </filesystem>
    <url name="shared">
      <ivy      pattern="http://releng.sanmateo.greenplum.com/ivy/modules/[organisation]/[module]/ivy-[revision].xml" />
      <artifact pattern="http://releng.sanmateo.greenplum.com/ivy/modules/[organisation]/[module]/[type]s/[artifact]-[revision].[ext]" />
    </url>                      
  </resolvers>

  <!-- ====================================================================== -->

  <triggers>
    <ant-call target="download-untar-trigger"
              prefix="dep"
              event="post-download-artifact"
              filter="type=targz AND status=successful"/>
  </triggers>

  <!-- ====================================================================== -->

</ivysettings>
